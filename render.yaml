services:
  # ── Frontend (static site) ────────────────────────────────────────────────
  - type: web
    name: enterpriseai-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_BACKEND_URL
        value: https://enterpriseai-backend.onrender.com        # update after backend deploys
      - key: VITE_RAG_URL
        value: https://enterpriseai-rag-backend.onrender.com   # update after rag-backend deploys

  # ── Agent backend (FastAPI, port 8000) ────────────────────────────────────
  - type: web
    name: enterpriseai-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false   # set manually in Render dashboard
      - key: ALLOWED_ORIGINS
        value: https://enterpriseai-frontend.onrender.com

  # ── RAG backend (FastAPI, port 8001) ──────────────────────────────────────
  - type: web
    name: enterpriseai-rag-backend
    runtime: python
    buildCommand: pip install -r rag-backend/requirements.txt
    startCommand: cd rag-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # set manually in Render dashboard
      - key: OPENAI_API_KEY
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_REDIRECT_URI
        value: https://enterpriseai-rag-backend.onrender.com/drive/callback
      - key: ALLOWED_ORIGINS
        value: https://enterpriseai-frontend.onrender.com
